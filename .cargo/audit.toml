# Canonical cargo-audit policy for DemonClaw.
# Every ignored advisory must be justified in SECURITY_EXCEPTIONS.md.

[advisories]
ignore = [
  "RUSTSEC-2023-0071",
]

[output]
deny = ["warnings", "unmaintained", "yanked"]
quiet = false
